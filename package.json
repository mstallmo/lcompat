{
  "name": "lcompat",
  "version": "0.0.1",
  "description": "Open source license compatability checker",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf ./bin && yarn run build:babel && yarn run copyJson",
    "build:babel": "babel . -d bin --ignore node_modules,test,bin,.build,temp,deployment --comments false",
    "copyJson": "cp -r ./definitions ./bin/",
    "local": "npm run build && sudo npm link",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": {
    "lcompat": "bin/index.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mstallmo/lcompat.git"
  },
  "author": "Mason Stallmo",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/mstallmo/lcompat/issues"
  },
  "homepage": "https://github.com/mstallmo/lcompat#readme",
  "devDependencies": {
    "@babel/cli": "^7.0.0",
    "@babel/core": "^7.0.0",
    "@babel/preset-env": "^7.0.0",
    "babel-eslint": "^9.0.0",
    "eslint": "^5.5.0",
    "eslint-plugin-import": "^2.14.0"
  },
  "dependencies": {
    "@babel/polyfill": "^7.0.0",
    "commander": "^2.18.0",
    "ora": "^3.0.0",
    "request": "^2.88.0",
    "request-promise": "^4.2.2"
  }
}
